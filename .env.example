# Copy to .env.development (local dev) or .env.production (deploy)
# Vite auto-loads .env.development for `npm run dev` and .env.production for `npm run build`

# Development: use /api (Vite proxy forwards to localhost:3001)
# Production: use full backend URL like https://your-api.com/api
VITE_API_URL=/api
